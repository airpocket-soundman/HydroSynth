# HydroSynth 実装仕様メモ

## 目的

HydroSynth は、ToF センサで取得した水面の波高変動をリアルタイムに解析し、外部音響系へ音声信号として出力する。

この文書は、実装時の判断基準として以下を整理する。

- センサ系の前提仕様
- 音声出力ハードウェア方針
- 波高測定から音声信号生成までの処理フロー
- リアルタイム処理の分担
- 初期実装で採るべきロジック

## 1. ハードウェア方針

前提ハードは `M5StickS3` とする。

### 1.1 センサ入力

- センサ方式: 単点 ToF
- 主候補: `VL53L4CD`
- 補助候補: `VL53L0X`, `VL6180X`
- 目標サンプリング: `100 Hz`
- 入力信号: 水面高さ `h(t)` [mm]

#### センサ接続仕様

- 物理コネクタ: `Port.A (HY2.0-4P / Grove)`
- バス種別: `I2C`
- 推奨ソフトウェアバス: `Wire / TwoWire(0)`
- `SDA = G9`
- `SCL = G10`
- 電源:
  - M5StickS3 Port.A 標準は `5V`
  - 3.3V 駆動したいモジュールは、モジュール仕様を確認して別配線する
- 主な I2C アドレス:
  - `VL53L0X = 0x29`
  - `VL53L4CD = 0x29`
  - `VL6180X = 0x29`

注意:

- `VL53L0X`, `VL53L4CD`, `VL6180X` はいずれも既定アドレスが `0x29`
- 差し替え運用は 4 線で成立する
- 同時接続する場合は `XSHUT / SHDN` などの追加制御が必要

理由:

- `VL53L4CD` は近距離で高速サンプリングしやすい
- `VL53L0X` は評価用としては使えるが速度面で不利
- `VL6180X` は超短距離向けで、本番用途の主候補にはしにくい

### 1.2 音声出力

テスト段階では内蔵スピーカを使ってよいが、本番は外部音声出力を前提にする。

推奨構成:

- `ESP32-S3 -> I2S -> 外部 DAC / I2S アンプ -> 外部アンプ or スピーカ`

優先順位:

1. `I2S DAC + ライン出力`
2. `I2S デジタルアンプ`
3. `内蔵スピーカ` はテスト専用

非推奨:

- PWM 疑似 DAC + RC フィルタ

理由:

- ノイズ、歪み、出力レベル管理の点で本番用途に向かない
- 外部アンプや PA へ接続しにくい

#### M5StickS3 内蔵音声系

M5StickS3 の内蔵音声は `ES8311` に接続されている。

- 内蔵 codec: `ES8311`
- 内蔵 audio 用 GPIO:
  - `MCLK = G18`
  - `DOUT = G14`
  - `BCLK = G17`
  - `LRCK = G15`
  - `DIN = G16`
  - codec 制御 I2C:
    - `SCL = G48`
    - `SDA = G47`

設計方針:

- 内蔵スピーカはテスト用として使用可
- 本番の外部音声出力は、上記内蔵 audio GPIO を使わず、別 GPIO に外部 I2S を出す

#### 外部音声出力の推奨 bus

- バス種別: `I2S`
- 推奨ポート: `I2S1`
- 推奨引き出し位置: `Hat2-Bus`

推奨理由:

- `Port.A` の `G9/G10` は ToF 用に確保したい
- 内蔵 ES8311 が使っている `G14/G15/G16/G17/G18` は避けたい
- `Hat2-Bus` 側は外部基板へ引き出しやすい

#### 外部 I2S 推奨ピン割り当て

本番用の初期推奨割り当ては以下とする。

- `I2S_BCLK = G5`
- `I2S_LRCK = G6`
- `I2S_DOUT = G7`
- `I2S_MCLK = 未使用`

補足:

- `PCM5102A` や `MAX98357A` のように `MCLK` 不要なモジュールを優先する
- `MCLK` が必要な codec を使う場合は、追加候補として `G8` を割り当てる
- 推奨引き出し位置の対応:
  - `G5`: Hat2-Bus pin 2
  - `G6`: Hat2-Bus pin 5
  - `G7`: Hat2-Bus pin 8
  - `G8`: Hat2-Bus pin 9

#### 外部 DAC / アンプ配線例

##### PCM5102A 系

- `M5StickS3 G5 -> PCM5102A BCK`
- `M5StickS3 G6 -> PCM5102A LRCK`
- `M5StickS3 G7 -> PCM5102A DIN`
- `3V3 -> VCC`
- `GND -> GND`

出力:

- DAC の `LINE OUT L/R` を外部アンプまたはミキサーへ

##### MAX98357A 系

- `M5StickS3 G5 -> MAX98357A BCLK`
- `M5StickS3 G6 -> MAX98357A LRC`
- `M5StickS3 G7 -> MAX98357A DIN`
- `5V or 3V3 -> VIN` はモジュール仕様に従う
- `GND -> GND`

出力:

- MAX98357A のスピーカ出力を外部スピーカへ

### 1.3 外部音声出力候補

#### A. ライン出力型

- 例: `PCM5102A` 系
- 用途: ミキサー、PA、録音系へ出したい場合
- 利点: 音質管理がしやすい

#### B. スピーカ直結型

- 例: `MAX98357A` 系
- 用途: 小型パワーアンプ一体で鳴らしたい場合
- 利点: 回路が単純

## 2. システム処理ブロック

HydroSynth の信号処理は、以下の 5 段に分ける。

1. センサ取得
2. 前処理
3. 特徴量抽出
4. 音パラメータ生成
5. 音声合成 / 出力

### 2.1 実装対象 bus 一覧

| 用途 | bus 名 | 物理線 | GPIO |
| --- | --- | --- | --- |
| ToF センサ | `I2C / Port.A` | `SDA` | `G9` |
| ToF センサ | `I2C / Port.A` | `SCL` | `G10` |
| 内蔵 audio codec | `I2S0 (internal)` | `MCLK` | `G18` |
| 内蔵 audio codec | `I2S0 (internal)` | `BCLK` | `G17` |
| 内蔵 audio codec | `I2S0 (internal)` | `LRCK` | `G15` |
| 内蔵 audio codec | `I2S0 (internal)` | `DIN` | `G16` |
| 内蔵 audio codec | `I2S0 (internal)` | `DOUT` | `G14` |
| 内蔵 audio codec 制御 | `I2C (internal)` | `SCL` | `G48` |
| 内蔵 audio codec 制御 | `I2C (internal)` | `SDA` | `G47` |
| 外部 DAC / アンプ | `I2S1 (external)` | `BCLK` | `G5` |
| 外部 DAC / アンプ | `I2S1 (external)` | `LRCK` | `G6` |
| 外部 DAC / アンプ | `I2S1 (external)` | `DOUT` | `G7` |
| 外部 DAC / アンプ | `I2S1 (external, optional)` | `MCLK` | `G8` |

## 3. 波高測定から音声信号までの変換フロー

### 3.1 センサ取得

ToF から水面までの距離 `d(t)` を取得する。

変換:

- 基準距離を `d0`
- 水面変位を `h(t) = d0 - d(t)`

ここで重要なのは、絶対距離そのものではなく、水面の上下変動成分を使うこと。

取得仕様:

- サンプリング周期: `10 ms`
- サンプリング周波数: `100 Hz`
- 各サンプルに時刻を付与
- `timeout` や `invalid` を別扱いで保持

### 3.2 前処理

入力系列 `h(t)` に対して、まず演奏向けの前処理を行う。

#### 必須

- DC 成分除去
- 欠損値処理
- 軽い平滑化

#### 推奨

- 平均水位の追従推定
- 外れ値クランプ
- 低域ドリフト除去

実装方針:

- 生データ系列と演奏用系列は分離する
- 記録やデバッグには生データを保持
- 音生成には前処理後の系列を使う

### 3.3 特徴量抽出

特徴量抽出は 2 系統に分ける。

#### A. 時間領域系

- 振幅
- 速度 `dh/dt`
- エネルギー
- 立ち上がり検出

役割:

- アタック
- 励起強度
- 瞬間的な変化の反応

#### B. 周波数領域系

- STFT によるスペクトル
- 主ピーク周波数
- スペクトル重心
- 帯域ごとのエネルギー

役割:

- 音高
- 音色
- ノイズ成分量

## 4. STFT 方針

### 4.1 基本方針

短いホップ幅で更新しつつ、窓長はある程度長めに取る。

推奨初期値:

- サンプルレート: `100 Hz`
- 窓長: `128 ms` から `200 ms`
- 更新周期: `10 ms`
- 窓関数: Hann

考え方:

- 長め窓で周波数成分を安定化
- 短い更新周期で時間分解能を確保

### 4.2 注意点

- 更新周期が `10 ms` でも、実効レイテンシは窓長の影響を受ける
- アタック感は STFT だけでは弱くなりやすい
- そのため、アタックは時間領域の変化量検出を別経路で扱う

### 4.3 計算量

`100 Hz` 入力では STFT の計算量は重くない。

ボトルネックになりやすいのはむしろ以下:

- 描画
- シリアル出力
- 音声合成

したがって、S3 上で

- センサ取得
- 特徴量抽出
- 音声出力

を並行させることは十分現実的である。

## 5. 音へのマッピング方針

単純に「距離 = 音高」とすると単調になりやすい。

HydroSynth では、以下のように役割分担するのがよい。

### 5.0 入力特徴量の定義

シンセ制御に使う入力特徴量を先に定義しておく。

| 記号 | 意味 | 主な算出元 | 役割 |
| --- | --- | --- | --- |
| `h(t)` | DC 除去後の水面変位 | `d0 - d(t)` | 基本信号 |
| `f_wave` | 主成分周波数 | FT / STFT の主ピーク | 音高の元 |
| `E_wave` | 主成分エネルギー | 主ピークのパワー / 振幅 | 音量の元 |
| `A_wave` | 波高包絡 | 短窓 RMS / abs 包絡 | 音量・フィルタ |
| `dh/dt` | 水位変化速度 | 時間差分 | アタック・変調 |
| `stability` | 周波数安定度 | 連続窓での `f_wave` 変動 | グライド量 |
| `width` | スペクトル幅 | 主ピーク周辺の広がり | 波形・明るさ |
| `event` | しずく等の立ち上がり | 微分・閾値検出 | 発音開始・強調 |

### 5.1 推奨マッピング

- 主ピーク周波数: 音高
- スペクトル重心: 明るさ / フィルタ開度
- 全体エネルギー: 音量
- 高域成分量: ノイズ量
- 立ち上がり量: アタック強度
- 緩やかな平均水位変化: ベースパラメータのドリフト

### 5.2 避けたい設計

- FFT 最大ピークだけで全部を決める
- 全帯域をそのまま音へ足し込む
- 生の距離値をそのまま音程へ直結する

理由:

- 単調化
- ノイズ化
- 不安定化

### 5.3 シンセ要素ごとの割り当て候補

以下は「どのシンセ要素に何を使うか」の全体整理である。

#### A. 音高系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| 基本音高 | `f_audio = f_wave * scale` | `f_wave` | 波紋周期と直結し自然 | 倍率設計が必要 |
| 音高オフセット | `base note + delta` | 平均水位、手の位置 | 演奏レンジを調整しやすい | 波紋性は弱くなる |
| グライド量 | portamento | `stability` | 揺れの不安定さを音で表現 | かけすぎると鈍い |
| ビブラート深さ | pitch modulation | `dh/dt` | 水の細かい揺れ感を足せる | ノイズで揺れやすい |

#### B. 音量系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| 基本音量 | gain | `E_wave` | 主成分の強さを素直に反映 | 実装の正規化が必要 |
| 包絡 | envelope level | `A_wave` | 直感的で扱いやすい | 帯域外ノイズの影響を受けやすい |
| アタック量 | transient gain | `event`, `dh/dt` | しずくの瞬間を強調できる | 誤検出対策が必要 |
| リリース長 | decay / release | `E_wave` の減衰速度 | 波が消える感じと同期しやすい | 長すぎると濁る |

#### C. 波形系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| 波形選択 | `sine / triangle / saw / square` | `width`, `event` | 水面状態で音色が変わる | 切替は不連続になりやすい |
| 波形モーフ | continuous morph | `width`, `A_wave` | 滑らかでシンセらしい | 実装が少し重い |
| パルス幅 | duty | `A_wave` | 力強さを出しやすい | 極端だと耳障り |
| 倍音量 | harmonic mix | `E_wave`, `width` | 明るさ制御に使いやすい | 調整幅が広い |

#### D. フィルタ系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| カットオフ | LPF cutoff | `A_wave`, `f_wave` | 強い波ほど明るくできる | 音量変化と混同しやすい |
| レゾナンス | resonance | `width`, `stability` | ピーク感を演出できる | かけすぎると発振的 |
| HPF 量 | low cut | 平均水位変動 | 低域の濁りを減らせる | 水らしさが減ることもある |

#### E. 空間系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| ディレイ時間 | delay time | `1 / f_wave` | 波周期と同期して自然 | 値が飛ぶと不安定 |
| フィードバック | delay feedback | `E_wave` | 強い波ほど余韻が残る | 濁りやすい |
| ディレイ mix | wet/dry | `A_wave` | 大きい波だけ空間感を増やせる | 常時深いと遠くなる |
| リバーブ量 | reverb send | `stability`, `E_wave` | 静かな波を広く演出できる | MCU負荷に注意 |

#### F. 変調系

| 制御対象 | 候補パラメータ | 向く特徴量 | 長所 | 注意 |
| --- | --- | --- | --- | --- |
| AM depth | tremolo depth | `A_wave` | 音量揺れと親和性が高い | 原信号と役割が重複しやすい |
| FM index | FM depth | `event`, `width` | しずくの瞬間に金属感を足せる | 強すぎると別物になる |
| コーラス深さ | detune / chorus | `stability` | 水の広がり感が出る | ぼやけやすい |
| ノイズ量 | noise blend | 高域成分量 | 水音らしさを足せる | 常時入れると汚れる |

### 5.4 波形バリエーション一覧

実装候補の波形と、HydroSynth との相性を整理する。

| 波形 | 音の印象 | 向いている入力 | 長所 | 注意 | 推奨度 |
| --- | --- | --- | --- | --- | --- |
| `Sine` | 純音、静か、透明 | `f_wave`, `E_wave` | 最も波紋らしい | 単調になりやすい | 高 |
| `Triangle` | 柔らかいが少し輪郭あり | `f_wave`, `width` | 純音より存在感がある | 少し地味 | 高 |
| `Saw` | 明るい、倍音多い | `A_wave`, `width` | 動きが分かりやすい | 荒れやすい | 中 |
| `Square` | 強い、電子的 | `event`, `dh/dt` | 立ち上がり強調に向く | 常時使うと硬い | 中 |
| `Pulse` | 独特、可変感あり | `A_wave`, `duty` | 演奏性がある | 調整が難しい | 中 |
| `Sine + Harmonics` | 水らしさを残しつつ華やか | `E_wave`, `width` | 本番向きの妥協点 | 実装管理が少し増える | 高 |
| `Noise mixed` | 水音寄り | 高域成分量 | 波紋ではなく飛沫感に向く | 入れすぎ注意 | 低〜中 |

### 5.5 エフェクトバリエーション一覧

| エフェクト | 主な制御対象 | 向く入力 | 長所 | 注意 | 推奨度 |
| --- | --- | --- | --- | --- | --- |
| ディレイ | time / feedback / mix | `f_wave`, `E_wave` | 波紋の反復感と相性がよい | 濁りやすい | 高 |
| フィードバックディレイ | feedback | `E_wave` | 波が広がる感じを出せる | 暴れやすい | 高 |
| 1pole LPF | cutoff | `A_wave` | 軽くて効きやすい | 劇的変化は出しにくい | 高 |
| エンベロープ | attack / decay / release | `event`, `E_wave` | 音の自然さが増す | 実装設計が必要 | 高 |
| コーラス | detune / depth | `stability` | 水面の広がり感 | ぼやけやすい | 中 |
| ビブラート | rate / depth | `dh/dt` | 生っぽい揺れ感 | 酔いやすい | 中 |
| AM / Tremolo | depth | `A_wave` | 波の脈動感を出せる | 元の包絡と重複しやすい | 中 |
| FM | index | `event`, `width` | しずくの瞬間に特徴が出る | 音色変化が強い | 低〜中 |
| リバーブ | send / decay | `stability`, `E_wave` | 空間感が出る | MCU負荷が増える | 中 |

### 5.6 おすすめ構成

実装のしやすさ、音の自然さ、調整のしやすさを考えると、初期おすすめは以下。

#### おすすめ 1: 最小構成

- 波形: `Sine`
- 音高: `f_audio = f_wave * scale`
- 音量: `E_wave`
- ゲート: `E_wave` の閾値

向く場面:

- まず波紋と音の対応を確認したいとき
- ノイズ要因を切り分けたいとき

#### おすすめ 2: 実用初期構成

- 波形: `Sine + 少量の 2次/3次倍音`
- 音高: `f_audio = f_wave * scale`
- 音量: `E_wave`
- フィルタ: `LPF cutoff = A_wave`
- エフェクト: 軽い `delay`

向く場面:

- デモ機
- インタラクティブ展示
- 波紋らしさと聴きやすさを両立したいとき

#### おすすめ 3: 表現重視構成

- 波形: `Sine <-> Triangle <-> Saw` のモーフ
- 音高: `f_audio = f_wave * scale`
- 音量: `E_wave`
- アタック: `event`
- フィルタ: `cutoff = A_wave`, `resonance = width`
- エフェクト: `delay + chorus`

向く場面:

- 楽器的に仕上げたいとき
- 展示より作品寄りにしたいとき

#### 非推奨構成

- 常時 `Square` や `Saw` だけで鳴らす
- `生の距離` を直接音高にする
- `全帯域エネルギー` をそのまま音量にする
- エフェクトを最初から多段に積む

理由:

- 水らしさが失われやすい
- ノイズが増幅されやすい
- 調整不能になりやすい

### 5.7 初期実装の具体的推奨

最初に実装すべきパラメータ割り当ては以下とする。

| 音要素 | 推奨入力 | 推奨ロジック |
| --- | --- | --- |
| 音高 | `f_wave` | `f_audio = f_wave * scale` |
| 音量 | `E_wave` | `0〜5mm 相当` を `0〜max gain` に写像 |
| 発音有無 | `E_wave` | noise floor 未満は無音 |
| 波形 | `sine` 固定 | まずは単純化 |
| 明るさ | `A_wave` | 余裕が出たら LPF cutoff へ |
| アタック | `event` | 余裕が出たら envelope へ |
| 空間感 | `f_wave` と `E_wave` | 余裕が出たら短い delay を追加 |

## 6. 初期実装ロジック

最初の音生成系は、複雑にしすぎない方がよい。

### 6.1 フェーズ 1

目的:

- 水面変動に対して音が自然に反応することを確認する

処理:

1. `h(t)` 取得
2. DC 除去
3. 振幅と速度を算出
4. 立ち上がり時に音を励起
5. 振幅で音量、速度でアタックを制御

音源:

- 減衰付きサイン波
- もしくは短い共鳴音モデル

### 6.2 フェーズ 2

目的:

- 波紋の周期感を音高へ反映する

処理:

1. STFT または簡易 DFT
2. 主周波数を抽出
3. 音高へ反映
4. スペクトル重心で音色調整

### 6.3 フェーズ 3

目的:

- 「波紋シンセらしさ」の形成

処理:

- 複数共鳴モードを持つ音源へ拡張
- 水面応答を modal synthesis 風に解釈
- 波の励起と減衰を音源内部状態へ反映

## 7. リアルタイム処理分担

### 7.1 タスク分離

推奨分担:

- タスク A: ToF サンプリング
- タスク B: 特徴量抽出
- タスク C: 音声生成 / I2S 出力
- タスク D: UI / グラフ / ログ

優先度方針:

- 最優先: 音声出力
- 次点: センサ取得
- 低優先: UI とログ

### 7.2 バッファ構成

- ToF サンプル用リングバッファ
- 前処理済み信号用リングバッファ
- 音声出力バッファ

注意:

- センサ欠損時はゼロ埋めではなく、直前値保持か invalid 扱いにする
- 欠損情報は別フラグで保持する

## 8. 実装上の優先順位

実装順は以下がよい。

1. `VL53L4CD` で `100 Hz` 安定取得
2. `h(t)` の DC 除去と平滑化
3. 立ち上がり検出ベースの簡易音源
4. 外部 I2S 音声出力
5. STFT ベース特徴量抽出
6. 音高・音色マッピング拡張

## 9. 現時点での判断

HydroSynth において本質的なのは「高性能な音響解析器」を作ることではなく、

- 水面変動の物理感
- 反応の速さ
- 音としての気持ちよさ

を両立させることである。

したがって、初期段階では

- センサは `VL53L4CD`
- 音声出力は `外部 I2S DAC`
- 解析は `時間領域 + 軽い周波数特徴`

を基本方針とし、STFT は必要な範囲で徐々に強くするのが妥当である。

## 10. コントローラ入力の実装計画

この章は、`M5StickS3` に複数の可変抵抗とトグルスイッチを追加し、HydroSynth の音作りパラメータをリアルタイム制御するための計画を整理したものである。

この段階ではまだコード実装は行わず、配線、役割、ソフトウェア構造、実装順を決める。

### 10.1 基本方針

- `Port.A` は ToF センサ専用として維持する
- `Hat2-Bus` 側の GPIO をコントローラ入力に使う
- `外部 I2S` を本番で使う前提なので、`G5/G6/G7` はできるだけ空ける
- 連続可変したい値は `アナログつまみ`
- 離散選択したい値は `デジタルスイッチ`

### 10.2 使う GPIO の整理

#### ToF 用に固定

- `G9`: Port.A SDA
- `G10`: Port.A SCL

#### 内蔵機能で避ける

- `G11`, `G12`: 本体ボタン
- `G14`, `G15`, `G16`, `G17`, `G18`: 内蔵 audio
- `G47`, `G48`: 内部 I2C

#### 外部 I2S 用に予約

- `G5`: BCLK
- `G6`: LRCK
- `G7`: DOUT

#### アナログ入力候補

- `G2`
- `G3`
- `G4`
- `G8`

#### デジタル入力候補

- `G43`
- `G44`

### 10.3 アナログつまみの推奨割り当て

本番前提の初期案は以下とする。

| GPIO | 種別 | 割り当て候補 | 役割 |
| --- | --- | --- | --- |
| `G2` | ADC | `pitch scale` | `f_wave` を何倍して可聴域へ上げるか |
| `G3` | ADC | `noise gate` | 小さな波をどこで無音にするか |
| `G4` | ADC | `delay mix` | 原音とディレイ音の混合比 |
| `G8` | ADC | `tone brightness / filter cutoff` | 音色の明るさ、LPF 開度 |

補足:

- 最初の実装では 4 個で十分
- `G5` も ADC としては使えるが、外部 I2S と競合するため初期案から外す

### 10.4 デジタルスイッチの推奨割り当て

波形選択はトグルまたはスライドスイッチで離散値として読む。

初期案:

| GPIO | 種別 | 役割 |
| --- | --- | --- |
| `G43` | Digital input | 波形選択 bit0 |
| `G44` | Digital input | 波形選択 bit1 |

2bit の割り当て:

- `00`: `sine`
- `01`: `triangle`
- `10`: `saw`
- `11`: `square`

### 10.5 アナログ入力の配線方針

各可変抵抗は通常の分圧として使う。

- 片端: `3V3`
- 片端: `GND`
- 中点: 対象 ADC GPIO

注意:

- ADC 入力は `0〜3.3V`
- `5V` を ADC GPIO に入れない
- 可変抵抗は `10kΩ` 前後を推奨

### 10.6 デジタル入力の配線方針

トグルスイッチは以下のどちらかで構成する。

#### A. 内部プルアップ利用

- GPIO を `INPUT_PULLUP`
- スイッチ ON で `GND` に落とす

利点:

- 部品点数が少ない
- 実装が簡単

#### B. 外部抵抗付き

- ノイズが気になる場合は `10kΩ` 程度でプルアップ / プルダウンを追加

### 10.7 ソフトウェア構造の計画

コントローラ入力は音声処理と分けて、低頻度で読む。

#### 読み取り周期

- アナログ: `10ms` から `20ms` ごと
- デジタル: `毎 loop` または `5ms` ごと

理由:

- 人間のつまみ操作は遅い
- センサや音声処理に比べて高頻度は不要

#### 内部データ構造案

```cpp
struct ControlState {
  float pitch_scale;
  float noise_gate_mm;
  float delay_mix;
  float filter_cutoff_norm;
  uint8_t waveform_index;
};
```

#### 処理分離案

1. ADC / Digital 読み取り
2. 生値を `0.0〜1.0` に正規化
3. 平滑化
4. 実パラメータへ変換
5. 音源・エフェクトへ適用

### 10.8 正規化と平滑化の計画

アナログ値は生で使わず、必ず正規化と平滑化を入れる。

#### 正規化

- ADC 生値を `0.0〜1.0` に変換
- 必要に応じてキャリブレーション範囲を持たせる

#### 平滑化

- `one-pole low pass`
- もしくは `moving average`

目的:

- つまみのガタつきを抑える
- 音への反映を滑らかにする

### 10.9 各つまみの具体的な変換ルール案

#### A. `pitch scale`

入力:

- `G2`

変換:

- `0.0〜1.0 -> 40〜200`

用途:

- `f_audio = f_wave * pitch_scale`

推奨:

- 最初は広すぎないレンジで始める

#### B. `noise gate`

入力:

- `G3`

変換:

- `0.0〜1.0 -> 0.1mm〜2.0mm`

用途:

- 主成分エネルギーまたは波高の無音判定

推奨:

- 小さめ側を広く取る

#### C. `delay mix`

入力:

- `G4`

変換:

- `0.0〜1.0 -> 0.0〜0.5`

用途:

- dry/wet バランス

推奨:

- 100% wet は初期実装では避ける

#### D. `brightness / cutoff`

入力:

- `G8`

変換:

- `0.0〜1.0 -> LPF cutoff の正規化値`

用途:

- 音色の明るさ

推奨:

- 指数カーブにすると扱いやすい

### 10.10 デジタルスイッチの具体的な変換ルール案

#### `waveform_index`

入力:

- `G43`, `G44`

変換:

- 2bit を 0〜3 の整数へ

用途:

- オシレータ波形を切り替える

対象:

- `0 = sine`
- `1 = triangle`
- `2 = saw`
- `3 = square`

### 10.11 追加で考えられるデジタル入力用途

`G43/G44` を波形選択に使うのが初期案だが、将来的には他にも割り当て可能。

候補:

- `delay on/off`
- `hold / freeze`
- `bypass`
- `internal / external audio mode`
- `quantize on/off`

ただし、初期段階では用途を増やしすぎない方がよい。

### 10.12 実装順序

推奨順:

1. `G2/G3/G4/G8` の ADC 読み取りだけ実装
2. 画面とシリアルに値を表示
3. `pitch scale` と `noise gate` だけ音源へ接続
4. `G43/G44` の 2bit 波形切替を追加
5. `delay mix` と `brightness` を反映
6. 必要なら追加スイッチを検討

### 10.13 初期実装のおすすめ

最初に入れるべきものは以下とする。

- アナログ 4 系統
  - `G2 = pitch scale`
  - `G3 = noise gate`
  - `G4 = delay mix`
  - `G8 = brightness`
- デジタル 2 系統
  - `G43/G44 = waveform select`

理由:

- 音の変化が分かりやすい
- 実装が複雑になりすぎない
- 外部 I2S との競合が少ない
